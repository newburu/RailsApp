<div class="body-container">
  <h2 class="page-explanation">登録が完了しました</h2>
  <%= render 'layouts/list-item'%>
  <div class="start_link"><%=link_to 'はじめる', energys_path, class: "start-btn", today_weight: current_user.days.create(weight: current_user.weight, date: Date.today)%></div>
</div>